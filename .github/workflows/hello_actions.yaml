# сначала добавим имя, которое увидим при запуске actions
name: Hello World 🌍
on:
    push:  # выполняем действие на push, отправка нового кода в репозиторий

# что нужно выполнить, какие действия выполняем, каждая joba состоит из шагов
jobs:
    # имя jobы
    hello-job:
        runs-on: ubuntu-latest
        steps:
            - name: My first step in GH Actions
              run: echo "Hello action!"
