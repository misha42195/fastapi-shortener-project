name: Hello World 🌍
on:
    push:

jobs:
    hello-job:
        runs-on: ubuntu-latest
        steps:
            -   name: My first step in GH Actions
                # получаем имя события из переменной и выведется имя события (при push)
                run: echo "Hello action on ${{ github.event_name }}"
