name: Python check 🐍
on:
    push:
        branches:
            - master
jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            -   name: Placeholder GH Action step
                run: echo "Run Action"
