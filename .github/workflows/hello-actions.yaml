name: Hello GH Actions
on:
    push:

jobs:
    hello-actions:
        runs-on: ubuntu-latest
        steps:
            -   name: My first step in GH Actions
                run: echo "Hello action!"
