name: Hello GH Actions 🌍

on:
    push:

jobs:
    Hello-world:
        runs-on: ubuntu-latest
        steps:
            -   name: My first step in GH Action
                run: echo "Hello actions!"
